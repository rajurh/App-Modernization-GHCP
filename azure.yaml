# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: eshoplite-storefx
metadata:
  template: eshoplite-storefx@0.0.1-beta

services:
  web:
    project: ./src/eShopLite.StoreFx
    language: dotnet
    host: containerapp
